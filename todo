custom callbacks in usefetch
(so we can handle errors differently for
specific components)

search loader
width fix on feed
profile photo uploader

friends panel size and row/cols

- test this with dummies

birthdate profile
friends notifications loader
